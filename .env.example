# Copy to .env (docker compose reads it automatically) and set a password
# to protect the web UI and the embedded Chrome login behind it.
# Leave blank/unset to run without a password (trusted LAN only).
WEBUI_PASSWORD=
